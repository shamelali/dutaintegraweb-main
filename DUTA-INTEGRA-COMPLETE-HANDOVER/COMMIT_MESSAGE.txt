security: harden vercel deploy, email API, and SEO - autonomous fix

Critical:
- Block public access to *.pptx, *.pdf, *.py, .claude/, Web/, admin.html via vercel.json redirects to 404
- Remove sensitive files from git cache (investor deck, financials, dev scripts)
- Harden api/send-email.js: hardcoded TO, origin check, rate limit 5/min, honeypot, validation, sanitization
- Add Cloudflare Turnstile verification (optional via env)

SEO:
- Add robots.txt blocking private paths
- Add sitemap.xml with 7 URLs + hreflang en/ms alternates
- Add security headers: X-Frame-Options DENY, HSTS, nosniff, Referrer-Policy

Maintainability:
- Create shared components/header.html + footer.html + loader JS to fix 6x duplication
- Secure contact forms (base + Turnstile version)
- Add .github/workflows/security.yml: gitleaks, npm audit, sensitive file blocker
- Update .gitignore to prevent future leaks

Env required: RESEND_API_KEY, TURNSTILE_SECRET_KEY (optional)

Fixes exposure of investor deck at /Duta_Integra_Solutions_Investor_Deck.pptx
