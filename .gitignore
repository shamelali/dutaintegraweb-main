# dependencies
/node_modules

# local env (keep the example)
.env*
!.env.example
/env
/env.*

# runtime data — inbound enquiries are stored here
/data/

# logs & debug
*.log
npm-debug.log*

# misc
.DS_Store
*.pem

# prevent git bundles from being committed
*.bundle
.vercel

# redundant backups (git has full history)
Web-backup-*/

# binary docs
*.docx

# personal tool configs
obsidian-knowledge-base/
